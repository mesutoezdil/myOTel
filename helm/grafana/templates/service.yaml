apiVersion: v1
kind: Service
metadata:
  name: ad-otel-grafana
  namespace: ad-otel
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ad-otel-grafana
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.port }}