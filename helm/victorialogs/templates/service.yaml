apiVersion: v1
kind: Service
metadata:
  name: ad-otel-victorialogs
  namespace: ad-otel
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ad-otel-victorialogs
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.port }}